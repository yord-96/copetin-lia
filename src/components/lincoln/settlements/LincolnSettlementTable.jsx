import { formatLincolnBs, formatLincolnDate } from '../shared/lincolnFormatters';

export default function LincolnSettlementTable({ rows = [], onOpen }) {
  return <div className="lincoln-phase3-table-wrap"><table className="lincoln-phase3-table"><thead><tr><th>Evento</th><th>Fecha</th><th>Cliente</th><th>Salón</th><th>Ingresos</th><th>Gastos</th><th>Utilidad</th><th>Estado</th><th></th></tr></thead><tbody>{rows.length ? rows.map((row) => <tr key={row.eventId}><td><strong className="lincoln-phase3-code">{row.eventCode || '—'}</strong><small>{row.eventType || 'Evento'}</small></td><td>{formatLincolnDate(row.eventDate)}</td><td><strong>{row.clientName || '—'}</strong></td><td>{row.roomName || '—'}</td><td className="is-money">{formatLincolnBs(row.operatingIncomeBs)}</td><td className="is-money">{formatLincolnBs(row.operatingExpenseBs)}</td><td className={`is-money is-utility-value ${Number(row.utilityBs) < 0 ? 'is-negative' : ''}`}>{formatLincolnBs(row.utilityBs)}</td><td><span className={`lincoln-phase3-status ${row.settlementStatus === 'closed' ? 'is-closed' : 'is-open'}`}>{row.settlementStatus === 'closed' ? 'Cerrada' : 'Abierta'}</span></td><td><button type="button" className="lincoln-phase3-open" onClick={() => onOpen(row.eventId)}>Abrir</button></td></tr>) : <tr><td colSpan="9" className="lincoln-phase3-empty-cell">No existen eventos para los filtros seleccionados.</td></tr>}</tbody></table></div>;
}
